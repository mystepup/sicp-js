// 인수가 여러개인 일반적인 경우에서 강제 형변환 처리
// 모든 인수를 첫 인수의 형식으로 변환, 두 번째 인수의 형식으로 변환 -> 이 전략이 충분히 일반적이지 않은 사례 제시
// 적절한 혼합 형식 연산이 테이블에 있긴 하지만 이 전략으로는 그 연산에 도달하지 못하는 경우를 찾아볼 것...

// A, B, C 세 가지 타입이 있을 때
// A와 C는 B로 강제 형변환 될 수 있다.
// (A, B, C)에 대한 연산을 평가할 때 (A, B, C)를 하거나 (B, B, B)만 평가할 테니
// (A, B, B)연산은 등록이 되어있지만 실행할 수 없다.